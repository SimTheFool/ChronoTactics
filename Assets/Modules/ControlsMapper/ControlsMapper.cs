using UnityEngine;

public class ControlsMapper
{
    private Controls controls;

    public ControlsMapper()
    {
        /* this.controls = new Controls();

        this.controls.Default.Enable();
        this.controls.Default.SelectSkill.performed += (context) => {
            Debug.Log("select skill");
        };

        this.controls.SkillSelected.Disable();
        this.controls.SkillSelected.ResolveSkill.performed += (context) => {
            Debug.Log("resolve");
        };
        this.controls.SkillSelected.CancelSkill.performed += (context) => {
            Debug.Log("cancel");
        }; */

        //this.controls.Default.SelectSkill.activeControl.
    }
}